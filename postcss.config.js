export default {
      plugins: {
        '@tailwindcss/postcss': {},
        // If you were previously using postcss-import or autoprefixer,
        // you might be able to remove them as v4 handles imports and
        // vendor prefixing automatically.
        // 'postcss-import': {},
        // 'autoprefixer': {},
      },
    };